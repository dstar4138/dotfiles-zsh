# Quick flags for editor and git.
alias v=vim
alias g=git

# Mistypes, i forget the space a lot.
alias ga="git a"
alias gs="git s"
alias sl=ls

# Some aliases for ease-of-use.
alias needs-restart="[ -f /var/run/restart-required ] && echo 'true'"
